# fraud-detection-system
End-to-end Credit Card Fraud Detection project using Machine Learning, FastAPI, and Streamlit. Includes data preprocessing, model training, API, and UI.
# Credit Card Fraud Detection System 💳

This project detects fraudulent credit card transactions using Machine Learning (Random Forest).

## 🚀 Technologies
- Python 3.13
- scikit-learn
- FastAPI (Backend API)
- Streamlit (Frontend UI)
- Pandas, NumPy, Matplotlib

## 📁 Project Structure
Explain briefly each folder.

## 🧠 How to Run
1. Clone the repo
2. Create venv & install requirements:
   ```bash
   pip install -r requirements.txt
3.Run API:
uvicorn api.main:app --reload
4.Run Streamlit:
streamlit run app.py
🧩 Model

The model was trained on the Credit Card Fraud Dataset.
